# Default dotfiles selections.

EXTERNAL_MODULE_REPOS=(
    # "myexternalmodule=atzufuki/myexternalmodule"
)

ACTIVE_MODULES=(
    # myexternalmodule
)

ACTIVE_SCRIPTS=(
    am
    gh
    llama-cpp
    opencode
    scarlett
    ssh
    telegram
    whatsapp
    zed
)
